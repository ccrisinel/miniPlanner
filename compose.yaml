services:
  miniplanner:
    image: ghcr.io/ccrisinel/miniplanner:latest
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - planner-data:/data

volumes:
  planner-data:
